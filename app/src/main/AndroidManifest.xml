<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Smartbudget">



        <!-- Launcher activity: MainActivity (login screen) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Other activities (NOT launched) -->
    <activity android:name=".CoordinatorLayoutActivity" android:exported="false" />
    <activity android:name=".FrameLayout" android:exported="false" />
    <activity android:name=".TableLayout" android:exported="false" />
    <activity android:name=".ConstraintLayout" android:exported="false" />
    <activity android:name=".GridLayout" android:exported="false" />
    <activity android:name=".MainActivity2" android:exported="false" />
    <activity android:name=".AddExpenseActivity" android:exported="false" />

    </application>

</manifest>
